@model IEnumerable<WdprPretparkDenhaag.Models.PlanningItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlanningId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TijdSlotId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AttractieId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dag)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlanningId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TijdSlotId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AttractieId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dag)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
